[project]
name = "hwutils"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "psutil-extra==0.2.0 ; python_full_version >= '3.12' and python_full_version < '4.0'",
    "psutil==6.0.0 ; python_full_version >= '3.12' and python_full_version < '4.0'",
    "psutil>=6.0.0",
    "pandas>=2.2.3",
]

[dependency-groups]
dev = [
    "sphinx>=8.2.3",
]
